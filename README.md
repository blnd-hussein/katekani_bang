# WXT + React

- Dev Server for Chrome:

```bash
pnpm dev
```

- Dev server for Firefox:

```bash
pnpm dev:firefox
```

## Packaging

 Make sure to update the app version in wxt.configs.ts
 
- For Chrome:

```bash
npx wxt zip
```

- For Firefox:

```bash

npx wxt zip -b firefox
```
